import { publishedRecords, SITE, recordPath } from "@/lib/records";
export function GET(){const paths=["/","/work","/film","/notebook","/research","/about",...publishedRecords().map(recordPath)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${SITE}${p}</loc></url>`).join("")}</urlset>`,{headers:{"Content-Type":"application/xml; charset=utf-8"}});}
