import Link from "next/link";
import {searchGraph} from "@/lib/graph";
import {allVideos,durationLabel} from "@/lib/youtube";
export const metadata={title:"Ask the work",description:"Search Chris Hay’s films and indexed transcript passages. Every result returns to its source."};
export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const q=((await searchParams).q||"").slice(0,500);const results=searchGraph(q);return <main id="main" className="ask-page"><p className="kicker record-voice">THE CONNECTED RECORD</p><h1>Ask<br/><em>the work.</em></h1><p className="dek">Find the film. Read the words. Return to the source.</p><form action="/ask" className="ask-form"><label htmlFor="ask-query" className="record-voice">SEARCH THE WORK</label><div><input id="ask-query" type="search" name="q" defaultValue={q} placeholder="What has Chris said about models as databases?" required maxLength={500}/><button type="submit" aria-label="Search the work">↗</button></div></form><div className="ask-prompts">{["LARQL","MCP apps","FFN","agent memory","quantization"].map(s=><Link key={s} href={`/ask?q=${encodeURIComponent(s)}`}>{s} ↗</Link>)}</div><p className="ask-coverage record-voice">{allVideos.length} FILMS · 2 TIMED TRANSCRIPTS · SOURCE SEARCH</p><p className="ask-explanation">This first edition retrieves source material rather than generating an answer. Automatic captions are unreviewed. Films without indexed captions can be found by their titles and available descriptions.</p>{q&&<section className="ask-results" aria-label="Search results"><p className="record-voice">{results.length} RESULTS FOR “{q}”</p>{results.map(r=><article key={r.id}><p className="record-voice">{r.basis==="automatic-caption"?`AUTOMATIC CAPTIONS / ${durationLabel(r.start!)}`:r.basis==="title-only"?"TITLE MATCH / TRANSCRIPT NOT INDEXED":"TITLE & DESCRIPTION"}</p><Link href={r.url}><h2>{r.title} ↗</h2></Link><p>{r.text}</p><a className="text-link" href={r.sourceUrl}>ORIGINAL SOURCE ↗</a></article>)}{results.length===0&&<p>No matching source found. Try a shorter phrase or a project name.</p>}</section>}</main>;}
