import { publishedRecords, SITE, recordPath } from "@/lib/records";
export function GET(){return Response.json({version:"https://jsonfeed.org/version/1.1",title:"Chris Hay — The record",home_page_url:SITE,feed_url:`${SITE}/feed.json`,description:"Published records by Chris Hay. Drafts are omitted.",items:publishedRecords().map(r=>({id:`${SITE}/records/${r.id}/${r.version}`,url:`${SITE}${recordPath(r)}`,title:r.title,content_text:r.abstract,date_published:`${r.published}T00:00:00Z`,authors:r.authors.map(name=>({name})),tags:r.concepts}))},{headers:{"Content-Type":"application/feed+json; charset=utf-8"}});}
