# Homepage notebook staging

The homepage entrance follows the authored exhibition notebook, especially
“The act before the shape”, “Information has dramaturgy” and “Exit · hold · enter”.
The current repair note asks for a comparison: the right page therefore shows
paired evidence, while the left page retains the published title and dek.

The shared HAUSE NotebookPreview remains the reading object. Its surrounding
scene belongs to this publication: more dark space around the book, directional
light, page-edge shadows and a restrained perspective. There is no automatic
motion, delayed text, scroll interception or content hidden until JavaScript runs.
Mobile removes perspective and stacks the pages. This is a rendered paper object,
not a photograph, and does not satisfy the separate photographic shoot brief.

The I12R plate reads the same evidence.json as the notebook. Blocks retain original
order 0–11. Written and executable treatments appear together for each world;
0/12 versus 7/12 is not a before/after transformation. The joint endpoint, protected
machinery and prose interface-compliance limits are visible beside the result.
The source link opens the existing paired-world record in the notebook.

Graph publication chronology still chooses the latest entry. Only
N-ECOLOGY-RECOVERY receives this experiment's plate. Every other entry receives
its existing conceptual marginal drawing, explicitly labelled as conceptual.
Collection colour and notebook format continue to follow the selected record.
The main reading link still opens the first page.

This also follows the editorial direction's requirements for real material,
source qualifications, direct reading and occasional changes of scale. It does
not impose a new opening on individual articles. The reusable notebook component
and its HAUSE source pin are unchanged; there is no new shared API to publish.

Validation: production build, full test suite, generated HTML/evidence checks.
The browser runtime returned no available browser; desktop/mobile visual and
keyboard review remain outstanding. No local server is needed for these checks.
