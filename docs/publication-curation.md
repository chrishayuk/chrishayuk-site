# Publication entrances

The homepage now follows Identity → Now → Current programmes → Latest → Systems → Film. Machine Discovery is the explicit Now selection. Latest is chronological and independent of that editorial choice. The existing house identity and social image are retained.

`lib/publication-index.ts` resolves the three research programmes and Practice from existing thread membership and explicit supplementary notes. Agent Ecology is a connected thread within the Machines entrance; the peer question remains discoverable in its archive. Programme titles are editorial groupings, not assertions that their experiments share a population or mechanism.

Notebook shows current threads and six latest notes. `/notebook/archive` includes every listed notebook, with server-rendered text and programme filters. Dates use first publication or creation, never a revision masquerading as a new publication; the existing editorial selection breaks same-day ties. Counts come from canonical records and exclude unlisted previews.

Research maps all three programmes, groups recorded findings into expandable lists, and retains the standalone open questions. Publication and outcome filters are independent. Outcomes come from explicit claim and evidence acts, including mixed findings within a single note. Sources and scope are available beside each result. No manuscript, publication snapshot, experiment identifier or outcome is rewritten by these indexes.
