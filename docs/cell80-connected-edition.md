# Cell80 connected edition — 11 September 2026

Reviewed the five existing visual notebooks alongside the machine-visit and
self-read editions, and incorporated the existing sixth draft and its supplied
evidence archive. The six notes now form one reading journey. This is an
editorial connection between different assays, not a continuous simulated world.

## Changes

- Added a six-note orientation strip, a question-and-finding map on the thread,
  and an explicit explanation of what each note makes us ask next.
- Connected all six through the canonical thread, Notebook selection, homepage,
  record headers and previous/next links. The last Cell80 entry returns to its
  own experiments instead of linking to the unrelated FFN graph question.
- Brought the EX-4 field-by-field intervention out of a disclosure into the
  main reading sequence, alongside the ancestry explanation.
- Added an EX-9 count diagram separating 12 started worlds, 11 surviving
  ecologies and zero eligible worlds passing either response screen.
- Added aligned EX-11 timelines showing why intake changes 19 and four ticks
  before establishment failed the timing rule.
- Added an EX-13 ten-comparison figure distinguishing six wins from four
  zero-offspring futures, with the eight-win requirement visible.
- Built an AP-2 instrument that switches between newly reachable programs and
  distinct functions. All nine modules tie at 27,888 programs; two controls
  exceed the evolved module's 17,962 newly reachable functions.
- Added a separate AP-2 comparison showing how its position changes from
  about the 76th percentile among same-shape modules to about the 49th among
  modules with similar output ranges. This assay counts functions built on a
  module, not the reachability endpoint in the preceding instrument.
- Preserved all original act anchors, draft states, earlier measurement data,
  and the EX-14 source discrepancy disclosure. EX-15's missing outcome is
  explicitly dated to the inspected snapshot rather than presented as a fresh
  research-status check. Added the existing closing draft's BOUND status to the
  site's domain vocabulary; HAUSE's existing domain-status fallback renders it.

## Evidence and verification

`lib/data/cell80-bound-controls.json` is a small extract of the supplied
`public/data/cell80/bound/ap2-encapsulation.json.gz`. The regression check compares
every chart value with that archive and verifies every file hash in the supplied
bound manifest, including the decompressed AP-2 hash. Source research files were
not edited. New diagrams label count scales, grouped ordering and gate criteria.

All 120 tests pass. TypeScript and focused ESLint pass (the existing homepage
image warning remains). The Sites production build passes. Local HTTP checks
verify the homepage, Notebook, thread, all six notes, every original act anchor,
all six navigation destinations on each note and 31 distinct evidence downloads.
No in-app browser or browser-opening tool was available; browser visual and
interaction QA has not been performed.

The unrelated pre-existing reciprocity operator log is outside this change.
