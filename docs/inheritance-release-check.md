# Inheritance notebook release check — 13 September 2026

Scope: “The stronger agent left something behind”, I1 and I2, plus its homepage,
ecology thread, Machines field map, graph, Ask and publication projections.

## Passed

- Registry reads confirm I1 and I2 are completed. Their report, protocol and full
  result copies match registered SHA-256 hashes. The historical Sol battery hash
  was verified before projecting its three source actions.
- All 18 I1 replay decisions match the raw result's world rows and stores. The
  inherited n1 is Sol's actual executed copy; generation resets and expiry match.
- I2 reproduces Qwen's 18 actions. Both inherited first generations earn seven;
  Gemma's removal control earns zero, with five MissingKey refusals and six literal
  READ n<id> parse failures across its panel. Those observations are retained.
- 154 tests passed, including both inheritance data tests and retrieval coverage.
  Type checking, changed-file lint and the production build passed.
- Legibility passes for 27 notebooks and four threads. Publication provenance
  passes for six preserved editions without changing earlier committed snapshots.
- The served-page checks verify the historical source, paired I1 replay, I2
  comparison, source downloads, citations and every incoming fragment link.
- Full local deployment checks pass, including all 27 notebook graph/Ask routes,
  metadata, feeds, sitemap and shared homepage/thread entrances.
- Fixed a stale maintenance fragment and replaced the field map's hard-coded
  three-note count with the thread's derived count.

## Limits of this review

The browser connection repeatedly reported “No browser is available”. Native
controls, focus styles, responsive rules and inherited contrast variables were
reviewed in source; the actual rendered layouts and playback interactions have
not been visually inspected. Do not describe this as completed visual QA.

Repository-wide lint remains at 33 errors and 18 warnings. A diagnostic comparison
against the pre-inheritance source, normalising paths, found no additions or
changes. The existing diagnostics include application files and bundled Hause
components; they were not suppressed or repaired as part of this publication.

External-reference advisories remain explicit: preserving a citation is not a
claim that a third-party page will remain available. Local experimental evidence
is preserved by content hash.

## Discovery cross-check

Registry write-up v5 matches the deployed discovery v1.1 findings. The live
manuscript hash is 74bf687605cd46a97bff53bfba9cae339185555a17b81a97801cfdbac056335d.
Subject 01's current terminal outcome is metric 2097, SUBSTITUTED; metric 2086 is
superseded. No recorded discovery outcome changed with the publication update.
TOOL-RECOGNITION and isolated-target discovery remain separate next experiments.
