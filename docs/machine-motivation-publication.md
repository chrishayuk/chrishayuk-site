# Publishing the motivation notebook

The visual edition of “The page could ask for a favour.” is the fifth machine notebook. Its reviewed content is published as N-MACHINE-MOTIVATION, version 1.0, dated 12 September 2026. The canonical record and SHA-256 are held in content/publications.json; the local manuscript and editorial audit remain in docs/notebook-drafts as development sources.

The homepage leads its machine section with the new note and its six-condition figure. The notebook index places it first. The machine journey links all five notes, with Authority-2 leading into motivation. Cell80 retains its separate six-note entrance.

The public graph and Ask use the same corpus. Every listed notebook has retrievable authored passages and source anchors. The human Ask page now calls retrieveGraph, as the search API already did, so natural-language questions receive the same narrowing behaviour; the page says when it matches only parts of a question. The private experimental machine-Ask configuration is unchanged.

Verification covers every listed notebook's graph node, passages, source links, Ask title retrieval and real HTTP citation targets. The new note additionally has checks for its first publication date, immutable hash, publication-only retrieval, RSS feeds, citation endpoint, social image, sitemap, homepage entrance and five-note journey. Natural-language checks cover courtesy, the runtime block and the interpretation pause.

The figures use only the completed MOTIVATION-2 dataset. The preregistered interpretation pause and post hoc release remain visible. No results from the later DISCLOSURE-1 series were added to this publication.

Validation before deployment: 123 automated tests passed, type checking and focused lint passed, both site and public production builds passed, and the complete production HTTP check passed. The HTTP check exercised all 20 listed notebook entries through the graph, search API, Ask page and every authored passage anchor. The new note also passed publication-only retrieval, feeds, versioned record, citation and generated social image checks.
