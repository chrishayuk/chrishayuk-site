# Recorded evidence contribution · 7 September 2026

ADDRESS-BUILD-1 supplies the first consumer of two shared publication capabilities: `EvidenceTable` and `MeasurementTrace`. The former replaces its complete reader, causal and transfer tables. The latter replaces the depth figure and participates in the existing MotionProvider. The causal transplant recreation and coordinate intervention remain site-owned.

The library contracts live in `vendor/hause/EVIDENCE.md`. Evidence data stays in `public/data/address-build-1`; the library contains no model names, layer numbers or experiment results. Units, rounding, missing observations, caution bands and source links are explicit. All measurements remain in native HTML tables; playback only advances between recorded stages.

The HAUSE publication page demonstrates both components using the same sourced record. Its ROUTING-2 evaluation uses EvidenceTable for actual selection outcomes. These are publication capabilities, not newly promoted forms or a claim of independent institutional reuse.

Vendored contribution revision: df8ab1ff58e71fd38ed51fe2fbe0a983080d23f5. The existing vendor's base revision and earlier publication contributions are preserved; only these component, contract and stylesheet files are mirrored from the new contribution.
