# Cell80 homepage and fifth note — 11 September 2026

The homepage keeps one featured Cell80 visual, followed by four compact title
links. Together they reach all five notes. The feature says “Start here”; the
full visual collection lives on `/thread/cell80`. It adds no extra homepage
players, charts or preview cards.

The fifth note now opens on the recorded history of child 1124, the single EX-13
candidate that passed both birth-time contexts. Playback covers all 2,835 recorded
steps from birth at tick 165 to tick 2999. Controls jump to transmission,
checkpoints, extinction and the endpoint. Amber marks count descendants that
continuously inherited both changes. They do not depict positions or persistent
individual identities. The original four-checkpoint chart remains in a disclosure.

The trace compares intact descendants with the whole world's population and its
organisms carrying both changes. Those whole-world counts include the selected
family. After extinction at tick 1268 the family has zero descendants, while
239 organisms elsewhere still carry both changes; 235 do at the endpoint.

`scripts/export-cell80-history.py` exports the relevant rows from the original
EX-13 JSONL, retains their exact bytes in a gzip download, and pins both the
selected-row hash and original-file hash. Population counts join the retention
trace and discovery history by their recorded tick. The test compares every
exported frame to those original rows, including founder exclusion and the
217 / 148 / 230 / 0 checkpoint counts.

The EX-14 grid now lets readers select each of its 100 paired futures and inspect
family births with and without higher intake. It continues using the original
local raw outcomes: 63 positive, one reproductive tie, 36 with neither version
reproducing. The report/raw discrepancy remains disclosed. This interaction does
not create new trials or independent discoveries.

Both further-note cards now show recorded visual summaries on the collection
page. The two population players share the same count-trace renderer, with
separate labels for the different quantities being compared.

Validation: all 98 tests passed, along with TypeScript and the production build.
Changed-file lint has no errors and one existing homepage image warning. Served
HTML confirms one homepage preview, four continuation links, all five note URLs,
100 selectable future controls, valid fragment targets and no nested buttons.
Production checks now cover the compact homepage and fifth-note player. Browser
interaction testing was not performed in this session.
