# Notebook / HAUSE audit — 9 September 2026

## Implemented after the cinematic review

The audit below describes the earlier state. The subsequent visual pass now:

| Entry | Improvement |
| --- | --- |
| N-STATE | Distinct rooms for the intervention, measured result, later branch and open persistence question; authored Comparison panels and one still Refusal. |
| N-ADDRESS | Seven-stage reading journey, six-fact table, calculation sequence, scoped probe/router/write evidence; original qualifications in citable field notes. |
| N-ATTRIBUTION | Restored H1 and all five graph anchors, exact record text in field notes, actual closing question, HAUSE Statement and coordinated TextCorrection. Correction now accurately says CI requires a rewrite, not that CI edits the commit. |
| N-MAP | Shared StudyRoom and StudySequence replace local presentation scaffolding; dark intervention/coordinate rooms and a contrasting document-to-authority turn. |
| N-AUTHORITY | A distinct dark source-preservation chapter, shared FieldNotes, static Refusal and native comparison readings. Original evidence matrices retained. |
| N-ADDRESS-BUILD | Existing visual experiments retained; supporting record moved to shared FieldNotes. |
| N-CONTEXT / N-OPERATOR | Compact proposition rooms and one explanatory distinction; explicitly open, without inventing an experiment. |

HAUSE source contribution: `7532558ee63b4a85741b0a1a2d4c2da980141fa2`.
See `vendor/hause/STUDIES.md`. The base vendor revision remains unchanged; the
eight contributed files match the authoritative source contribution.

Browser checks covered all eight entries at 320, 390, 820, 1440 and 1920px,
including opened field notes, one H1 and all 99 act destinations. Keyboard
comparisons retain stage height with and without JavaScript. TextCorrection was
checked for coordinated pause/replay and reduced-motion resting state. The
research records, source URLs, experiment values and film starts were not edited.

Still separate work: authority matrix consolidation with EvidenceTable, the
publication-local summary capability, and any future domain-specific instrument
extraction. Those are not claimed as completed by this presentation pass.

## Original audit

Reviewed all eight existing public Notebook entries in source and in the local
browser at 1440px and 390px, with reduced motion. This is an audit, not a claim
that the older entries have all been migrated. No older article was rewritten.

| Entry | Current HAUSE use | Finding |
| --- | --- | --- |
| N-ATTRIBUTION | Shared tokens and publication apparatus; body is custom | Highest-priority migration. No H1, none of its five `act-N` targets exists, and its erasure performance has an independent observer rather than shared MotionProvider ownership. |
| N-ADDRESS-BUILD | EvidenceTable, MeasurementTrace, shared motion, citation apparatus | Strong adoption. All six act targets resolve. Model-specific transplant and coordinate studies correctly own their data locally. Review reusable interaction contracts before further extraction. |
| N-MAP | Acts adapter routes to HAUSE forms; shared film/motion | All 23 act targets resolve. Research diagrams stay local. Field-note disclosure and static refusal are reusable local extensions awaiting contribution. |
| N-STATE | Acts adapter uses Statement, Observation, Comparison, Evidence, Refusal, Question | All nine targets resolve. Uses the shared forms directly. |
| N-ADDRESS | Acts adapter, HAUSE forms and shared film/motion | All 23 targets resolve. The addressed-memory invitation is a publication-specific extension. |
| N-AUTHORITY | HAUSE forms via Acts, shared motion, recorded-arm studies | All 28 targets resolve. Static refusal, disclosure, summary, and some evidence table patterns are still local candidates for HAUSE. |
| N-CONTEXT | Statement, Observation, Question via Acts | All three targets resolve. Uses the shared forms directly. |
| N-OPERATOR | Statement and Question via Acts | Both targets resolve. Uses the shared forms directly. |

No entry caused horizontal page scrolling at either tested width. The depth
note's mobile MeasurementTrace extends into its authored full-width frame, but
does not widen the document. This does not establish every interactive state,
contrast ratio or browser combination.

## Follow-up order

1. Restore the attribution note's H1 and five graph citation destinations, then
   adopt HAUSE's appropriate statement/refusal/code forms. Contribute the reusable
   erasure behaviour and move it under the shared motion owner.
2. Add a backwards-compatible static Refusal presentation in HAUSE and replace
   `NotebookRefusal`; do not flatten away status, exact lines or the principle.
3. Contribute the citable field-note disclosure used by three notebooks; retain
   native disclosure and automatic opening when an act fragment is followed.
4. Review the authority evidence matrices against EvidenceTable. Preserve missing
   observations, experiment identities and source precision when adapting them.
5. Keep domain-specific drawings and research data in their publications. Reuse
   proven HAUSE contracts rather than turning every unique diagram into a form.

The exhibition note now consumes shared HAUSE exhibition frames, the room mode
of Statement, authored panels of Comparison, and StagedTransition. The source
contribution and consumer mirror are documented in
`docs/hause-exhibition-contribution.md`.
