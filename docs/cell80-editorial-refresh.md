# Cell80 editorial refresh — 10 September 2026

The three working notes now open with a short invitation and the instrument.
Prior work follows the visual sequence. The opening study uses the existing
HAUSE StudyRoom, with a compact heading and introduction; technical detail uses
native FieldNotes. Original act anchors are retained. The historical replay and
EX-9/EX-10 measurements are unchanged.

The invention entry opens on EX-14, then follows the earlier EX-10 movement
experiment through the separate EX-11 metabolic world and EX-12/13 opportunity
and transmission experiments. The homepage, reading thread and semantic record
now describe that progression. All three entries remain drafts.

## Source check

Read full chuk-experiments records for EX-11 through EX-15 and compared EX-11–14
with RESULTS.md and INTERPRETATION.md in the corresponding ecology-research
directories in chris-experiments. EX-15 is still marked running, has no conclusion
or reported outcome, and its local preregistration supplies no result. No database
or source-research files were changed.

EX-14's written report and database conclusion conflict with its local raw file:

| Quantity | Database / report | Local ex14.jsonl |
| --- | ---: | ---: |
| B futures with lineage births | 41/100 | 22/100 |
| BC futures with lineage births | 64/100 | 64/100 |
| Positive BC−B among reproductive futures | 63/64 | 63/64 |
| Conditional ties | 0 | 1 |
| Conditional losses | 1 | 0 |
| Neither B nor BC reproduces | 36/100 | 36/100 |

The conditional mean (99.453125) and median (4) also agree. This discrepancy is
unresolved: the check does not establish why the summaries and raw file differ.
The visual is explicitly derived from the local file. The source disclosure
records both accounts and links the original report, database snapshot and raw
file. Both unconditional and conditional denominators stay visible.

`lib/data/cell80-followups.json` contains all 100 seed-indexed paired birth counts
and SHA-256 hashes of the copied source files. Its public twin is identical.
The static grid preserves seed order and has a textual equivalent. Regression
checks compare every displayed row with the copied raw source, its recorded
effect and event flag, and verify all source hashes.

## Evidence boundaries

- EX-11: online composition crossed the engineered resource barrier; its
  registered retention gate failed and dependence assays had no eligible case.
- EX-12: one of four candidates passed both contexts; the supported child left
  no offspring. Independent replication was not established.
- EX-13: one of five supported at birth, transmission and intact descendants
  observed; the later 8/10 gate failed at 6/10 with four zero-offspring ties.
- EX-14: one descendant checkpoint, 100 paired futures. Conditional support is
  not 100 independent discoveries and does not retroactively pass EX-13's gate.
- EX-15: replication pending. A further dependent step, historical necessity,
  environmental mediation and open-ended evolution remain untested.

## Validation

All 88 tests pass, including the EX-14 raw-evidence checks. TypeScript and focused
ESLint pass; the homepage retains its existing image-optimization warning. The
production build passes. Local HTTP checks return 200 for all three notes, retain
33 / 29 / 38 unique act anchors, and resolve their evidence links. The rendered
EX-14 grid has exactly 63 helpful, one tied and 36 unrealized marks.

No connected browser was available, so visual and interaction QA could not be
completed. The refreshed three-note edition was deployed on 10 September 2026
through the existing GitHub-to-Fly workflow, and its live revision, pages and
evidence download were verified.


## Further notes

The later completed experiments support two focused companion drafts:

- `N-CELL80-BARRIER`: **Give invention something to unlock.** EX-11's resource
  barrier, online composition, all four control arms and the two ineligible early
  uptake events. The first figure pairs 0/89 primitives with 1/1,825 compositions.
- `N-CELL80-HISTORY`: **An advantage needs a chance to become history.** EX-12's
  unrealized opportunity, EX-13's transmitted cohort and EX-14's conditional
  effect. The first figure follows 217, 148, 230 and zero intact descendants;
  the later figure reuses the verified 100 paired EX-14 futures.

These remain distinct from the original three-question sequence, with links in
its Further Notes section, the Notebook collection and the invention record.
Both have complete semantic records, source references and stable act anchors.
The source catalogue and local files still supply no completed EX-15 result.

TypeScript, focused ESLint, the full test suite and the Sites build pass. Local
HTTP checks verify both pages, nine and ten unique act anchors, draft labels,
source links, both sequence links, and the exact EX-14 outcome marks. No connected
browser was available for visual QA.
